extends ./main.pug

block head
  style.
    main {
      font-family: monospace;
      background: var(--red-300);
      border: 1px solid;
      color: var(--red-100);
      border-radius: 3px;
    }

block main
  p= error
